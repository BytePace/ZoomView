import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    topOverflow: {
        overflow: 'hidden',
        zIndex: 1,
    },
    scrolledContainer: {
    },
});

export default styles;
